[workspace]
resolver = "2"
members = [
    "crates/pg_resp",
    "crates/resp-proto",
    "crates/resp-store",
    "crates/resp-client",
]

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"
opt-level = 3
lto = "fat"
codegen-units = 1
